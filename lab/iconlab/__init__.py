"""iconlab — model fine-tuning & eval lab for the icon-labeler extension.

See lab/README.md and icon-labeler-extension-plan.md §5. The shared preprocessing
contract (preprocess.json) and label map (labels.json) must stay identical to the
extension side; everything else here is lab-internal.
"""

__version__ = "0.1.0"
